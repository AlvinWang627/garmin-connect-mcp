# Garmin Connect Credentials
GARMIN_EMAIL=your_email@example.com
GARMIN_PASSWORD=your_password

# Optional authentication token to protect your Cloudflare Worker URL
AUTH_TOKEN=your_custom_secret_bearer_token

# Required for the remote MFA enrollment endpoints when using a Worker
MFA_ADMIN_TOKEN=your_one_time_setup_admin_token
